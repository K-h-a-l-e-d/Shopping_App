import 'package:flutter/material.dart';

ValueNotifier<bool> isLoggedIn = ValueNotifier<bool>(false);

//Setting theme mode initially as dark
ValueNotifier<bool> isDarkThemeEnabled = ValueNotifier<bool>(true);
